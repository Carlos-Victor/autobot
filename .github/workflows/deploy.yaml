name: Deploy via ArgoCD

on:
  workflow_dispatch:  # será chamado automaticamente pelo build.yaml

env:
  APP_NAME: autobot
  ARGOCD_SERVER: ${{ secrets.ARGOCD_SERVER }}
  ARGOCD_TOKEN: ${{ secrets.ARGOCD_TOKEN }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Install ArgoCD CLI
        run: |
          curl -sSL -o /usr/local/bin/argocd https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-amd64
          chmod +x /usr/local/bin/argocd

      - name: Sync app via ArgoCD
        run: |
          argocd app create $APP_NAME \
            --dest-namespace $APP_NAME \
            --dest-server https://kubernetes.default.svc \
            --helm-set image.tag=${{ github.sha }} \
            --helm-set secret.DISCORD_TOKEN=${{ secrets.DISCORD_TOKEN }} \
            --project default \
            --revision 0.1.0 \
            --server ${ARGOCD_SERVER} \
            --upsert --grpc-web \
            --auth-token ${ARGOCD_TOKEN} \
            --sync-option CreateNamespace=true \
            --values-literal-file helm/values.yaml

          argocd app sync $APP_NAME \
            --server ${ARGOCD_SERVER} \
            --force \
            --retry-limit 3 \
            --grpc-web \
            --auth-token ${ARGOCD_TOKEN} \
            --timeout 2100
