apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "autobot.fullname" . }}-test-connection"
  labels:
    {{- include "autobot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "autobot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
